[tool.black]
line-length = 120
target-version = ["py312"]
extend-exclude = "migrations"

[tool.isort]
profile = "black"
line_length = 120
skip_glob = ["*/migrations/*"]
known_first_party = ["apps", "config"]
