[pytest]
DJANGO_SETTINGS_MODULE = config.settings.development
python_files = test_*.py
testpaths = apps
addopts = --tb=short -q --strict-markers
markers =
    integration: exercises the database or external services
