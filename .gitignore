# Secrets
.env
infra/.env
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
staticfiles/
media/
.coverage
htmlcov/
.pytest_cache/

# Node / React
node_modules/
web/dist/
web/.vite/

# Flutter
mobile/build/
mobile/.dart_tool/

# Editors / OS
.DS_Store
.idea/
*.swp

# Claude Code local state (settings.json is shared and IS committed if present)
.claude/settings.local.json
.claude/*.tmp.*

# Local verification screenshots (scripts/shoot.mjs)
.shots/

# A newer revision of the WLT handoff bundle, unpacked at the repo root. Ignored
# rather than deleted: it carries a 33-step linkage workflow document, a
# filterable matrix and seven policy parameters that docs/wlt_module_handoff/
# does not have, so merging it into the committed copy is a spec decision
# somebody has to make rather than a tidy-up.
/wlt_module_handoff-2/
